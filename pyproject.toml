[project]
name = "temporal-index"
version = "2.0.0"
description = "Hybrid retrieval-augmented chat with local LLM generation"
readme = "README.md"
requires-python = ">=3.11"

[tool.setuptools]
py-modules = ["paths"]
